<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bottomsup.mapbox.app">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/final_app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Gray">
        <activity
            android:name="bottomsup.mapbox.app.activity.Bottom"
            android:label="@string/title_activity_bottom"></activity>
        <activity android:name="bottomsup.mapbox.app.activity.ThemePickerActivity" />
        <activity android:name="bottomsup.mapbox.app.activity.MainActivity" />
        <activity android:name="bottomsup.mapbox.app.activity.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>